$OpenBSD: patch-src_lib-imap_imap-id_c,v 1.1.2.1 2011/05/30 21:38:56 sthen Exp $

IMAP: Fixed ID command to log the parameters properly.

--- src/lib-imap/imap-id.c.orig	Sun May 29 18:08:15 2011
+++ src/lib-imap/imap-id.c	Sun May 29 18:09:39 2011
@@ -169,6 +169,7 @@ const char *imap_id_args_get_log_reply(const struct im
 			str_append_c(reply, '=');
 			str_append(reply, str_sanitize(value, 80));
 		}
+		args++;
 	}
 	return str_len(reply) == 0 ? NULL : str_c(reply);
 }
